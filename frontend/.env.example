# Leave unset. The frontend makes same-origin relative requests that Vite proxies
# to the backend (see vite.config.ts), which keeps the session cookie first-party.
# Setting this to a different origin requires CORS and breaks cookie auth in
# browsers that strip third-party cookies.
# VITE_API_BASE_URL=
